/* Store several names under a single variable */
let students = ['Farhah', 'Girish', 'Humma', 'Andrea', 'Kenny', 'Ben', 'Ayaanle', 'Julia', 'Corey']

/* Print every name stored in that array */
for (i = 0; i < students.length; i++) {
    console.log(students[i])
}

for (i = 0; i < students.length; i++) {
    console.log('Great to see you, ' + students[i] + '!')
}